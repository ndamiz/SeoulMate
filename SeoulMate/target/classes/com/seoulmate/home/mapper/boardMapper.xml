<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.seoulmate.home.board.BoardDAOImp">
 	<select id="comAllRecord" resultType="com.seoulmate.home.board.BoardVO">
  		select no, category, subject, userid, hit, to_char(writedate, 'MM-DD HH:MI') writedate from community order by no desc
  	</select>
  	<insert id="boardInsert" parameterType="com.seoulmate.home.board.BoardVO">
  		insert into community(no, userid, category, subject, content, ip)
  		values(comsq.nextval, #{userid}, #{category}, #{subject}, #{content}, #{ip})
  	</insert>
</mapper>